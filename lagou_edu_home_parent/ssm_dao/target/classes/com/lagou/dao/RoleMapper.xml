<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lagou.dao.RoleMapper">

    <!--查询所有角色（条件）-->
    <select id="findAllRole" parameterType="com.lagou.domain.Role" resultType="com.lagou.domain.Role">

        select * from roles
        <where>
            <if test="name !=null and name != '' ">
                and name = #{name}
            </if>
        </where>

    </select>

    <!-- 根据角色ID查询关联的菜单信息ID public List<Integer> findMenuByRoleId(Integer roleid);-->
    <select id="findMenuByRoleId" resultType="int" parameterType="int">
        SELECT m.id
            FROM roles r INNER JOIN role_menu_relation rm ON r.id = rm.role_id
	                     INNER JOIN menu m ON m.id = rm.menu_id WHERE r.id =#{id}
    </select>

    <!--根据roleid删除在中间表与菜单的关联关系-->
    <delete id="deleteRoleContextMenu" parameterType="int">
        delete  from role_menu_relation where role_id = #{rid}
    </delete>

    <!--为角色分配菜单-->
    <insert id="roleContextMenu" parameterType="com.lagou.domain.Role_menu_relation">
        insert into role_menu_relation values(null,#{menuId},#{roleId},#{createdTime},#{updatedTime},#{createdBy},#{updatedby})
    </insert>

    <!--删除角色     public void deleteRole(Integer roleid);-->
    <delete id="deleteRole" parameterType="int">
        delete from roles where id = #{roleid}
    </delete>

    <!-- 查询当前角色拥有的资源分类信息 public List<ResourceCategory> findAllResourceCategoryByRoleID(Integer id);   -->
    <select id="findAllResourceCategoryByRoleID" parameterType="int" resultType="com.lagou.domain.ResourceCategory">
        SELECT * FROM resource_category rc WHERE rc.`id` IN
          (
            SELECT DISTINCT r.category_id
            FROM resource r INNER JOIN role_resource_relation rrr ON rrr.`resource_id` = r.`id`
            WHERE rrr.role_id = #{roleId}
          )
    </select>

    <!--查询当前角色拥有的资源信息public List<Resource> findAllResourceByRoleID(Integer id);-->
    <select id="findAllResourceByRoleID" parameterType="int" resultType="com.lagou.domain.Resource">
        SELECT r.*
        FROM roles ro INNER JOIN role_resource_relation rr ON ro.id = rr.role_id
		INNER JOIN resource r ON r.id = rr.resource_id WHERE ro.id = #{id}
    </select>

    <!--根据角色ID 删除角色与资源的关联关系public void deleteRoleResourceRelation(Integer roleId);    -->
    <delete id="deleteRoleResourceRelation" parameterType="int">
        DELETE FROM role_resource_relation WHERE role_id = #{roleId}
    </delete>

    <!--插入最新的关联关系public void saveRoleResourceRelation(Role_Resource_Relation role_resource_relation);-->
    <insert id="saveRoleResourceRelation" parameterType="com.lagou.domain.Role_Resource_Relation">
        insert into role_resource_relation values (null,#{resourceId},#{roleId},#{createdTime},#{updatedTime},#{createdBy},#{updatedBy})
    </insert>

</mapper>